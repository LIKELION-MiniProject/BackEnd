# PassPort / RoadMaker — 코워크 인수인계 문서

> **작성일** 2026-07-17 (코드 제출 마감일) · **발표** 7/20
> **용도** 다른 코워크(또 다른 세션/계정)에서 이 작업을 **누락 없이 이어받기** 위한 단일 인수인계 파일.
> 원칙: 이 문서보다 **저장소 실제 설정**과 **팀 원본 `API_명세서_v2.md` / Notion**이 SSOT. 충돌 시 실제값 우선.

---

## 0. 프로젝트 한 줄 요약

수강 이력을 입력하면 → **졸업 요건 달성도를 규칙 기반으로 진단**하고 → **다음 학기 과목을 AI가 추천**하는 개인화 학사 대시보드. 서비스명 **PassPort**(Notion/발표 표기 **RoadMaker**).

- 대상 학과: **빅데이터 인공지능 전공** 1개 하드코딩
- 플랫폼: **Web 전용**
- 절대 원칙: **사실 판정(졸업가능/학점/GPA/요건충족)은 전부 Java 규칙 코드**. AI는 오직 ① 수강 이력 텍스트 파싱 ② 추천 과목 + 이유 설명에만.

### 팀
| 역할 | 이름 |
|---|---|
| PM · BE | 송원석 (원준, 본인) |
| BE | 이은종 |
| FE | 황인서 (인서) |
| 디자인 | 임신영 |

---

## 1. 저장소 · 폴더 · 도구 현황 (어디에 무엇이 있나)

### 1-1. 백엔드 저장소 (읽기 기준, 이미 존재)
- 경로: `C:\Users\송원석\Documents\LikeLion_MiniProject_Team_3\src\main\java\com\passport\**`
- 스택: **Java 21 + Spring Boot**, Spring Security + JWT + BCrypt, Spring Data JPA, H2 파일모드 → MySQL(`feat/mysql` 브랜치), 배포는 nginx + AWS EC2 예정.
- 상태: **전체 정독 리뷰 완료. 크래시성 버그 없음.** (상세는 §3-1)
- `CLAUDE.md`(프로젝트 루트): 개발 명세 SSOT. 이 문서와 함께 참조.

### 1-2. 프론트엔드
- 스택: **React 19.2.7 / React DOM 19.2.7 / React Router DOM 7.18.1 / Vite 8.1.1 / @vitejs/plugin-react / ESLint**
- FE 저장소 브랜치(팀): `feature/auth-api` → `feature/connect` 로 머지 예정.
- **주의: 이번 세션에서 만든 FE 코드는 아직 FE 저장소가 아니라 `outputs`(임시 스크래치)에 있음.** 다음 코워크가 FE 저장소로 옮겨야 함. (§3-3, §2 목록)

### 1-3. 산출물 위치
- **임시 작업(outputs, 사용자 화면엔 안 보임)**: 이번 세션에서 만든 FE 코드·핸드오프 MD가 여기 있음.
- **공유 폴더(사용자가 보는 곳)**: `C:\Users\송원석\Documents\LikeLion_MiniProject_Team_3` — 최종 산출물은 반드시 여기로. 이 인수인계 파일도 여기 저장됨.

### 1-4. 연결 도구 (MCP)
- **Notion**: RoadMaker 워크스페이스 = **멋쟁이사자처럼 미니프로젝트 3조**. (fetch/search/query-data-sources/create-pages/update-page/move-pages 사용 가능. **delete/archive 툴은 없음** → 삭제는 move-pages로 홀딩 페이지 이동 후 사용자가 휴지통 처리.)
- **Figma**: 멋사 미니 프로젝트 3팀 파일 (로그인/홈/성적/AI 분석/마이페이지 5화면).
- **Notion 재인증 필요할 수 있음**: 비대화형 세션에선 OAuth 불가 → 사용자가 claude.ai 커넥터 설정 또는 `/mcp`로 인증.

---

## 2. 이번 세션에서 만든 파일 목록 (outputs)

> ⚠️ 이 파일들은 **outputs(임시)** 에 있음. 최종적으로 FE 저장소 또는 공유 폴더로 이동 필요.

**FE 화면 (React)**
- `AiAnalysisPage.jsx` — AI 분석 (기본 보기 / 분석 비교하기 토글, **2개 방향 비교**, 실제 API fetch 버전)
- `GradeDashboardPage.jsx` — 성적 대시보드
- `RequirementsInputPage.jsx` — 졸업 요건 입력
- `CourseHistoryPage.jsx` — 수강 내역 입력
- `LoginPage.jsx` · `SignupPage.jsx`
- `App.jsx` (라우터) · `AuthContext.jsx` · `ProtectedRoute.jsx`

**FE API 래퍼 (src/api)**
- `client.js` (공통 fetch) · `auth.js` (토큰 저장/signup/login/me)
- `profile.js` · `requirements.js` · `courses.js`(BE 계약 정렬 완료) · `diagnosis.js` · `recommendations.js`

**핸드오프/기획 문서 (MD)**
- 진행상황/플랜 세트(08, 20–25, 30–38, 40–42, 62–64 번대), `제출_체크리스트_20260717.md`, `Notion_RoadMaker_점검_소넷프롬프트.md` 등.

---

## 3. 지금까지 한 일 (완료)

### 3-1. 백엔드 전체 코드 리뷰 — 결론: 크래시성 버그 없음, 견고
확인된 핵심 계약(요약):
- `SignupRequest{email, password(≥8), nickname}` · `LoginResponse{accessToken, tokenType:"Bearer", userId, nickname}` · `MeResponse{userId, email, nickname, profileId}`
- `Course{ credit: **double**, category(enum명), grade(A0/B0 정규화→A/B), year:int, semester:int, retake:bool, MAJOR_BASIC 포함 }`
- `CourseResponse{id, name, credit(double), category, grade, year, semester, retake}`
- `CertificationResponse{type, status}` · `CertificationUpdateRequest{items:[{type, status}]}`
- `GpaTrendResponse{semesters:[{year, semester, gpa, earnedCredit, predicted}], cumulativeGpa, categoryGpa:[{key,label,gpa}]}`
- `DiagnosisResponse`(graduationCertification 포함) · `DashboardResponse{overallProgress, categories, semester, requestedCredits, earnedCredits, semesterGpa, courses}`
- 에러 응답 = `{code, message}` (flat) / 성공 = `ApiResponse{success, data, message}`
- 추천 계약 v2.1: `RecommendationResponse{directions[], recommendations[](mirror), defaultDirectionId, source(live/cache/fallback), generatedAt, persona}`, `DirectionDto{directionId, name, description, caution, thin, recommendations[]}`, `RecoItemDto{courseCode, courseName, credit, category, reasons[3]}`

**리뷰에서 플래그한 통합/운영 이슈 6건** (버그 아님, 확인·조정 필요):
1. `ProfileUpdateRequest` 11필드 **전체 교체(full-replace)** → FE가 일부만 보내면 null로 덮일 위험.
2. `Course.credit`이 **double**인데 FE 드롭다운은 int → 직렬화/표시 정렬 필요(FE `courses.js`는 정렬됨).
3. `persona` / `graduationCertification` 필드를 FE가 아직 안 씀.
4. persona 서브프로세스 호출이 course-CRUD 트랜잭션 안에 있어 지연 위험.
5. `semesterGpa` 네이밍(대시보드) — FE에서 `dash.gpa`가 아니라 `dash.semesterGpa` 사용해야 함.
6. **live 추천엔 python + Gemini 필요** (passport-ai). 없으면 cache/fallback 경로로.

### 3-2. Figma ↔ 코드 1:1 대조
- 5화면(로그인/홈/성적/AI 분석/마이페이지) 데이터 대조 완료.
- **홈 / 마이페이지는 팀 스코프 결정으로 컷**(커밋으로 확인). Figma엔 있으나 API/화면 제외.
- 남은 갭: 홈 "이번 학기 우선 목표" BE 소스 없음, 필수과목 N건 = `requiredCourse.required(null)` 필요, 마이 "나이"는 Profile에 없음, `deptCode → 한글 학과명` 매핑 필요.

### 3-3. 프론트엔드 코드 산출 (실제 API 연동 버전)
- AI 분석 비교 화면: **5개 전부 한 번에 보여주지 않고, 2개 방향만 비교**하는 방식으로 구현(신영 질문 반영).
- 로그인/온보딩 라우팅: `GET /auth/me`의 `profileId` 유무로 온보딩↔대시보드 분기.
- API 래퍼 전부 BE 실제 계약(doc 37)에 맞춰 작성. `courses.js`는 카테고리 한글↔enum, grade 라벨, 학기 문자열↔year/semester 변환까지 정렬 완료.

### 3-4. Notion RoadMaker 정리 (대부분 완료)
- **빈 페이지 채움**: `백엔드 구현`(38ec…8090), `프론트엔드 구현`(38ec…8036), `ERD 다이어그램`(38ec…8080, Mermaid erDiagram) + ERD 링크 연결.
- **API 명세서 DB (21 페이지)**: 전부 실제 계약(v2.1) + 에러 포맷 `{code, message}` + ErrorCode로 본문 재작성. AI 행 상태 정정, 요건 저장/조회 행 추가.
- **기능 명세서 DB (7/13 세트)**: 전 항목 본문을 통일 양식으로 작성 — `## 요구사항 / ## 주요 동작 / ## 연결 클래스(Controller·Service·Domain/DTO) / ## 규칙·예외 / ## 연관 API`. 졸업 요건 입력 기능 추가.
- **중복 제거 완료**: 옛 6/29 빈 세트(18행) + 방금 발견한 `PASS-REQ-002 졸업 요건 입력` 빈 중복 1행 = **총 19행을 홀딩 페이지로 이동**.
  - 홀딩 페이지: **"삭제 대상 - 옛 기능명세 중복(6/29)"** `id 3a0c6804-1c5f-8170-b9a6-d7845104c445` (RoadMaker 홈 하위).
  - **기능 명세서 테이블은 이제 7/13 채워진 세트만 남아 깨끗함.**
  - ⚠️ **남은 액션(사용자)**: 홀딩 페이지를 **Notion 휴지통으로 이동**하면 영구 삭제(휴지통 30일 보관). 커넥터엔 삭제 툴이 없어 사용자가 직접 해야 함.

### 3-5. 제출 준비 점검
- 로컬 git 확인: BackEnd `main` 푸시됨, `feat/mysql`은 앞서 있으나 **미머지**.
- 제출 체크리스트 문서(`제출_체크리스트_20260717.md`) 작성.

---

## 4. 앞으로 해야 할 일 (오늘 7/17 마감)

### 4-1. 사용자(원준)가 직접 처리 (권한/클릭 필요 — 코워크가 대신 안 함)
- [ ] **Notion 홀딩 페이지 "삭제 대상 …" 휴지통으로 이동** (중복 영구 삭제 마무리).
- [ ] BackEnd 브랜치 결정: `main` 제출 vs `feat/mysql` 머지 후 제출.
- [ ] FrontEnd `feature/auth-api → feature/connect` 머지 확인.
- [ ] eulji@likelion.org(은종) 등 협업자 Notion/저장소 초대.
- [ ] 최종 기획서 **PDF export**(RoadMaker Notion에서).
- [ ] Google Form 제출.
- [ ] git push / `.\gradlew test`는 Windows에서 본인이.

### 4-2. 남은 FE 통합 (다음 코워크가 처리 가능)
- [ ] outputs의 FE 파일들을 **FE 저장소로 이동**.
- [ ] `GradeDashboardPage`: `dash.gpa` → **`dash.semesterGpa`** 로 수정(§3-1 이슈 5).
- [ ] **프로필 생성 화면** 연결(온보딩): `POST /profiles`.
- [ ] `ProfileUpdate`는 **전체 객체 전송**(부분 전송 시 null 덮임 방지, §3-1 이슈 1).
- [ ] deptCode → 한글 학과명 매핑 유틸.

### 4-3. BE 운영/엣지 (제출·발표 전 확인)
- [ ] live 추천 경로: python + Gemini(passport-ai) 준비 여부. 없으면 cache/fallback로 데모.
- [ ] persona 서브프로세스가 course-CRUD 트랜잭션 지연 유발하지 않는지.
- [ ] 발표 재현성: 예시 학생 시드 + 추천 캐시, 429/503 폴백 경로 테스트(원칙 2·부록 D).
- [ ] Gemini 키는 **passport-ai `.env`에만**. 절대 커밋 금지.

### 4-4. 발표 준비 (7/20)
- [ ] PPT 작성(7/19 목표), E2E 리허설(로그인→입력→진단→추천 최소 1회 통과).

---

## 5. 계약(SSOT) 핵심 요약 — 빠른 참조

- **Base URL** `/api/v1`, `application/json; charset=UTF-8`.
- 비회원 허용: `POST /auth/signup`, `POST /auth/login`. 그 외 전부 `Authorization: Bearer <JWT>`.
- `/profiles/{id}/**`는 **본인 소유 검증**(타인 리소스 403 = PROFILE-003).
- 엔드포인트(9도메인): auth(signup/login/logout/me), profiles(POST/GET/PATCH), courses(POST/GET/PUT/DELETE, **1건 단위**), certifications(GET/PUT), diagnosis(GET·규칙), gpa-trend(GET·규칙), recommendations(POST 생성/GET 캐시·AI), requirements(GET 하드코딩 + 유저저장 PUT/GET), dashboard(GET 집계).
- Grade 표기: JSON은 `"A+"`, 내부는 환산값(A+4.5…F0.0, P/NP는 GPA 제외 = 분자·분모 모두 제외).
- category enum: `MAJOR_REQUIRED / MAJOR_ELECTIVE / MAJOR_BASIC / GE_REQUIRED / GE_ELECTIVE / GENERAL_ELECTIVE`.

---

## 6. 지난 세션 교훈 · 함정 (다음 코워크 필독)

1. **Notion 편집은 반드시 실증하라.** "수정했다"고 단정하지 말고 query로 확인 후 **직접 URL**을 제시할 것. 지난번 옛/새 중복 행 때문에 사용자가 계속 빈 행을 열어 "반영 안 됨"으로 오인 → 큰 혼선. (지금은 중복 제거됨.)
2. **워크스페이스 확인**: 편집 전 대상 DB가 맞는지(collection id) 확인. 기능 명세서 DB = `069c6804-1c5f-8241-b5bc-87f2151726dd`. API 명세서 DB는 별도(8b7c…).
3. **outputs 큰 파일 Write 잘림(NUL) 이슈**: 대용량은 bash heredoc로 쓰고 esbuild 컴파일로 검증. (bash가 다운될 수 있으니 유의.)
4. **보안**: `.env`/시크릿 커밋 금지, `application.yml`은 `${ENV_VAR}`만. "보안 누출 걱정 없는 것만" 채운다(API 명세는 시크릿 없어 안전).
5. **삭제 툴 없음**: Notion 삭제는 move-pages로 홀딩 이동 후 사용자가 휴지통.
6. **사용자가 직접 하는 일**: 초대·폼 제출·PR 머지·git push·gradlew test는 클릭을 사용자가. 코워크는 대신 실행하지 않음.

---

## 7. 다음 코워크 즉시 시작 프롬프트 (복붙용)

```
멋사 미니프로젝트 3조 PassPort/RoadMaker 이어받는다.
이 폴더의 "인수인계_코워크_20260717.md"와 CLAUDE.md를 먼저 읽어라.
오늘 코드 제출 마감(7/17). §4-2 남은 FE 통합부터 진행하되,
Notion 편집은 반드시 query로 실증 후 직접 URL 제시. 워크스페이스=멋사 미니프로젝트 3조.
사실 판정은 규칙 코드, AI는 파싱/추천만. 시크릿 커밋 금지.
```

# 95 — DoD(제출 완료 정의) 체크리스트 (2026-07-16 작성)

> **용도** : 7/17 20:00 코드 제출 직전, "제출해도 되는가"를 한 화면에서 판정하기 위한 체크리스트.
> **선행** : [93 현황](93_세션인계_현황스냅샷_20260716.md) · [94 잔여작업](94_세션인계_잔여작업_20260716.md)
> **상태 표기** : ✅ 완료·검증 / 🟡 준비됨·미검증 / 🔴 미완(사람 블록) / ⏳ 예정

---

## 0. 제출 판정 요약

**현재는 제출 불가.** 코어 백엔드·배포·데이터·캐시는 끝났으나, 아래 **게이트 3개**(FE 쓰기 연동 / nginx 외부배포 / 리허설 3회)가 남아 있다. 이 셋이 모두 ✅가 되면 제출 가능.

| 게이트 | 상태 |
|---|---|
| G1. FE 쓰기 연동(수강 POST/PUT/DELETE) 동작 | 🔴 인서 블록 |
| G2. nginx 외부 공개 주소 접속 | 🟢 **거의 완료**. nginx 설치·설정, 포트80 개방(이미 열려있었음), **외부 접속 `http://15.164.84.176/` 200 확인**, API 프록시 외부 동작 확인. 남은 것 1개 → **인서 `dist` 배치**(현재 임시 안내페이지) |
| G3. 리허설 3회 통과 | ⏳ G1·G2 이후 |

---

## 1. 기능 완결성 (E2E)

| # | 항목 | 상태 | 근거·비고 |
|---|---|---|---|
| 1 | 로그인 → JWT 발급 → 인증 보호 API 접근 | ✅ | 7/16 demo1 로그인·토큰·403 소유검증 실증 |
| 2 | 요건 조회(`GET /requirements/BIGDATA_AI`) | ✅ | `130/21/39/14/21/35`, 어학·봉사 필수 반환 확인 |
| 3 | 수강 이력 CRUD (POST/GET/PUT/DELETE) | ✅ 서버 / 🔴 FE | 백엔드는 7/15 라이브 실증. FE 쓰기 연동 미완(94 §2) |
| 4 | 진단(규칙) — 달성도·부족학점 | ✅ | 39/130, 졸업가능=False |
| 5 | GPA 트렌드(규칙) — 학기별 | ✅ | 누적 3.8158, 2026-1 성적표 일치 |
| 6 | AI 추천 5방향 + 이유 3개 | ✅ | 캐시 `source=live`, 23항목 reasons 3개 |
| 7 | 대시보드 집계 (오표시 수정 반영) | ✅ | `exam=미입력`, `coreLiberal=미입력` |
| 8 | **FE 진입 시 GET 로드 → 화면 표시** | 🟡 | 읽기 연동은 사실상 완성(GPA·persona 표시 확인) |
| 9 | **FE 수강 추가/수정/삭제 → 서버 반영** | 🔴 | **핵심 미완.** 인서 작업(94 §2 표 항목2) |
| 10 | "AI 분석" 버튼 → POST → 재분석 | 🔴 | 인서 작업(94 §2 표 항목4) |

---

## 2. 안정성·폴백 (발표 사고 방지)

| # | 항목 | 상태 | 근거·비고 |
|---|---|---|---|
| 1 | Gemini `429/503` 시 캐시 폴백 | ✅ | GET은 캐시 반환. `source` 유지 로직 확인(94 §4-⑤) |
| 2 | 브릿지 타임아웃/강제종료 방어 | ✅ | PassportAiBridge 60s / PersonaBridge 20s + destroyForcibly |
| 3 | `source=fallback` 배지 시연 경로 | 🟡 | `.env` 리네임 방식만 가능. demo2 시딩 방식은 미결정 #1 |
| 4 | 서버 자동 복구 | ✅ | systemd `Restart=always` |
| 5 | 롤백 지점 확보 | ✅ | 구 jar(EC2) + DB·캐시 백업(로컬). ⚠️ 코드만 롤백 금지(credit 컬럼) |

---

## 3. 보안·시크릿

| # | 항목 | 상태 | 근거·비고 |
|---|---|---|---|
| 1 | `.env`·키 커밋 이력 없음 | ✅ | `.gitignore:49` `.env` 차단. 추적 파일은 `.env.example`(플레이스홀더)뿐. history에 `.env` 커밋 없음 |
| 2 | `.pem` 키페어 미추적 | ✅ | `git ls-files` 매칭 없음 |
| 3 | `application.yml` 시크릿 `${ENV}` 참조만 | ✅ | `jwt.secret=${JWT_SECRET}` 등, 하드코딩 없음 |
| 4 | h2-console 운영 차단 | ✅ | EC2 `SPRING_H2_CONSOLE_ENABLED=false` |
| 5 | 최종 커밋 전 시크릿 스캔 | ⏳ | 🔴 **스캔 시 `.env` 제외**(git diff --cached 대상). 94 §4-⑦ |

---

## 4. 문서·제출물

| # | 항목 | 상태 | 근거·비고 |
|---|---|---|---|
| 1 | README 로컬 실행 순서 | ✅ | "빠른 시작" 3단계 + 데모 계정 |
| 2 | README 배포 주소·구조 | ✅ | nginx 공개주소 `http://15.164.84.176/` + 구조 기재. 실제 nginx 배포 후 접속 확인만 남음 |
| 3 | README "판정은 규칙, AI는 선택·설명" | ✅ | 핵심 원칙 섹션 |
| 4 | README 임의값 데이터 고지 | ✅ | "데이터 고지" 섹션(성적·강의평가·요건) |
| 5 | mocks/ 최신화 | 🔴 | `PassPort_검증_mocks생성.ps1` 실행 대기(원석 비번) |
| 6 | 게이트·컷 판단 기록 | 🟡 | 93·94에 분산 기록. 컷 항목: GRADE_SAFE thin 수용, C안 폐기, 카드숨김 철회 |
| 7 | DoD 체크리스트(이 문서) | ✅ | — |

---

## 5. 제출 직전 순서 (G1·G2·G3 통과 후)

1. 🔴 **시크릿 스캔** — `git diff --cached` 대상, `.env` 제외
2. ⏳ **README 배포 주소 갱신** — nginx `:80` 확정값으로
3. ⏳ **최종 커밋·push** — 작은 단위, `feat:`/`docs:` 접두어
4. ⏳ **20:00 제출** (마감 22:00, 2시간 버퍼) + Slack 3줄
5. ⏳ 발표(7/20) 후: EC2 종료 + **탄력적 IP 릴리스**(미연결 EIP 과금)

---

## 6. 제출 보류 사유 (오늘 기준)

- **G1 (FE 쓰기 연동)** 이 없으면 "과목 라이브 추가 → 진단 즉시 반영" 시연이 불가 → 발표 핵심 흐름 결손.
- **G2 (nginx)** 없이 `:8080` 직접 노출은 가능하나, 시연 시나리오·CORS·mixed content 관점에서 same-origin 공개가 안전.
- **G3 (리허설 3회)** 전까지 코드 수정 동결(94 §7). 리허설이 실제 회귀 검증을 겸한다.

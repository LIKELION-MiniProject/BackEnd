# 미니프로젝트 제출 체크리스트 & 가이드 — 2026-07-17

> **마감** : 오늘(7/17). 발표 14:30~17:30. **제출 마감 이후 수정 시 실격.**
> **접근 권한** : 접근 권한 있는 모든 것은 **`eulji@likelion.org`** 초대 필수.
> **제출 구글폼** : https://forms.gle/XxUqvCwUapn1DSXg6

---

## 1. 오늘 제출할 것 (구글폼 3종)

| # | 항목 | 대상 | 현재 상태 | 마감 전 할 일 |
|---|---|---|---|---|
| 1 | **Backend 코드** | `github.com/LIKELION-MiniProject/BackEnd` | `main` 배포·동결 ✅ · `feat/mysql`(MySQL 검증+리포트) · 문서 2건 미커밋 | 문서 커밋 정리 → **레포 링크 제출** |
| 2 | **Frontend 코드** | `github.com/LIKELION-MiniProject/FrontEnd` | `feature/auth-api`에 전부 커밋·푸시됨(온보딩 포함), **아직 미머지** | **PR 머지** → **dist 빌드·배포** → 레포 링크 제출 |
| 3 | **UX/UI 디자인** | Figma (신영) | 링크 확인 필요 | Figma **공유 링크** 제출 + eulji 초대 |
| 4 | **최종 기획 문서** | PDF | ✅ **초안 완성** → [`docs/PassPort_최종기획문서.pdf`](PassPort_최종기획문서.pdf) (6페이지) | **내용 검수**(팀원 이름·역할·학교명 등) → 필요 시 최신 진행상황 반영 → 제출 |

---

## 2. 추후 제출 (구글폼 아님, 별도)

| 항목 | 마감 | 준비물 |
|---|---|---|
| **발표 자료 PPT** | 07.19 | 발표 덱 (H2 선택·MySQL 검증·데이터 이원화 리포트 활용 가능) |
| **서버·도메인 영수증** | ~07.26 | AWS EC2(t3.small, EIP `15.164.84.176`) 결제 내역 · 도메인 구입 시 그 영수증 |

---

## 3. 접근 권한 부여 — `eulji@likelion.org` 초대 (⚠️ 본인이 직접)

> **이 작업은 각 서비스 UI에서 계정 소유자가 직접 해야 함**(권한 변경은 대신 못 함, 나는 초대 버튼을 못 누름).
> 아래는 저장소·문서 전수 스캔으로 **우리가 실제로 쓰는 플랫폼만** 확인한 결과.

### ✅ 반드시 초대 (3곳)

| # | 플랫폼 | 대상 | 초대 위치 |
|---|---|---|---|
| 1 | **GitHub** | 조직 `LIKELION-MiniProject` (BackEnd·FrontEnd 레포 **둘 다 한 번에 커버**) | 조직 Settings → People → Invite member (owner 권한 필요) — 안 되면 레포별 Settings → Collaborators |
| 2 | **Figma** | 디자인 파일 (신영) | 파일 우상단 Share → 이메일 초대 (뷰 권한 이상) |
| 3 | **Notion** | 프로젝트 워크스페이스/페이지 | 워크스페이스 Settings → Members, 또는 페이지 Share |

- [ ] GitHub 조직 초대 완료
- [ ] Figma 초대 완료
- [ ] Notion 초대 완료

### ⚠️ 실제 사용 여부 확인 후 필요 시 초대

| 플랫폼 | 확인할 것 |
|---|---|
| **Google Drive** | 기획 문서 원본·에셋을 Drive에 뒀는가? 안 썼으면 불필요 |
| **Slack** | 팀 전용 워크스페이스에 제출 관련 산출물이 있는가? 보통 불필요 |

### ❌ 초대 대상 아님 (확인 완료)

| 항목 | 이유 |
|---|---|
| **Google AI Studio (Gemini API 키)** | 개인 API 키 — 초대 개념 없음. 코드·git에도 미포함 |
| **AWS EC2 서버 자체** | 구글폼 초대 대상 아님. **영수증만 추후 제출**(~7/26) |

---

## 4. 제출 순서 (오늘, 위에서부터)

### A. 코드 확정 (개발자)
1. **[FE] PR 머지** — `feature/connect ← feature/auth-api` (clean fast-forward 확인됨, 102 문서). 제출 기준 브랜치(main 등) 정책 확정 후 반영.
2. **[FE] dist 빌드·배포** — `.env.production` **비움**(⚠️ `/api/v1` 넣으면 이중경로 404, 103 문서 §2) → `npm run build` → `dist/`를 원석에게 → nginx 교체.
3. **[FE→BE] 라이브 확인** — `http://15.164.84.176/` 에서 로그인→온보딩→성적→AI 한 바퀴.
4. **[BE] main 최종 확인** — 배포본과 일치·동결. 미커밋 문서(101 FE·데이터 이원화 리포트) 커밋 위치 결정.
5. **[BE] `feat/mysql` 처리 결정** — main 병합 X 권장(검증 브랜치로 유지). 리포트는 제출 참고자료.

### B. 문서·디자인
6. **최종 기획 문서 PDF** 취합·export.
7. **Figma** 정리·공유 링크 확보.
8. **Notion 최신화** 확인 (현행 내용 반영).

### C. 권한 + 제출
9. **`eulji@likelion.org` 초대** (§3 전부).
10. **구글폼 제출** — BE 레포·FE 레포·Figma 링크·기획 PDF. (⚠️ 제출 후 수정 금지)

### D. 추후
11. PPT (07.19) · 영수증 (~07.26).

---

## 5. GitHub/Notion 최신화 점검 결과

### Backend (직접 확인함)
- `main` = 배포·동결본 ✅ (원격 동기화됨)
- `feat/mysql` = MySQL 전환 검증 + 리포트 4종 (커밋·푸시됨)
- **미커밋 (커밋 안 하기로 결정, 로컬 참고자료로 유지)**:
  `docs/101_FE_현황_및_다음작업_다른세션용.md` · `docs/데이터저장_이원화_설계리포트.md` · `docs/데이터저장_이원화_비전공자용.md` · `docs/PassPort_최종기획문서.pdf` · `docs/제출_체크리스트_및_가이드_20260717.md`(본 파일) · `인수인계_코워크_20260717.md`
  → 코드·서버엔 영향 없음. **PDF만 제출용으로 별도 첨부**하면 되고, 나머지는 커밋 여부와 무관하게 제출에 지장 없음.
- `.Rhistory`(무관 파일, 무시)
- ✅ 서버 라이브 정상 (nginx 200 / API 401 정상)

### Frontend (102·103 문서 기준)
- `feature/auth-api`에 전 작업 커밋·푸시 (401·성적 CRUD·재조회·AI·**온보딩 신규**·`.env.production`)
- **미머지** — PR 링크: `github.com/LIKELION-MiniProject/FrontEnd/compare/feature/connect...feature/auth-api`
- 커밋: `dff9f88`(.env.production) · `7d17a20`(온보딩) · `3d43291`(API 통합)

### Notion
- ⚠️ **이 세션에서 접근 불가**(커넥터 미인증). **본인 또는 Cowork(노션 연결)에서 최신화 확인 필요.**

---

## 6. 결정 필요 항목 (FE 102·103 + BE)

| # | 항목 | 출처 | 권장 |
|---|---|---|---|
| 1 | FE PR 머지 대상 브랜치 | 102 | `feature/connect` FF 머지, 제출은 최종 브랜치 링크 |
| 2 | `PassPort_API명세서.xlsx` 레포 포함 | 102·103 | 비밀번호·토큰 없음 확인됨 → 포함해도 무방(원하면 `git rm`) |
| 3 | `analysis.js USE_MOCK=true`(진단/전략/강점 화면 mock) | 103 | 시간 없으면 **그대로 두고 발표서 "과목추천은 실연동, 진단화면은 목업" 명시** |
| 4 | 학과 코드 `BIGDATA_AI`만 | 103 | 발표는 이 전공으로 시연 → 유지 |
| 5 | 백엔드 TLS(HTTPS) 부재 | 102·103 보안 | 오늘은 차단 사유 아님. **영수증/도메인 단계(07.26)에 도메인+Let's Encrypt 검토** |
| 6 | prod DB 테스트 계정 2개 정리 | 103 §5 | 시연 무관 → 발표 후 정리(급하지 않음) |
| 7 | BE 미커밋 문서 커밋 위치 | 본 문서 §5 | `feat/mysql`에 묶어 커밋(발표 자료) |

---

## 7. Cowork에서 하는 법

Cowork(커넥터 연결된 Claude)에서 **문서·레포 상태 점검과 취합**을 도울 수 있음. 단 아래는 **반드시 본인이 직접**:
- 권한 초대(`eulji@likelion.org`) — 권한 변경은 대신 못 함
- 구글폼 제출 — 최종 제출 클릭은 본인
- PR 머지 클릭(정책 확정 후)

**Cowork가 도울 수 있는 것**: Notion 페이지 최신화 확인·정리, GitHub 브랜치/커밋 상태 확인, 기획 문서 초안 취합, 제출 체크리스트 추적.
→ 사전 조건: Cowork에서 **Notion·GitHub 커넥터 연결(인증)** 되어 있어야 함.

---

## 8. 한 장 요약

```
[오늘 제출]  BE레포 · FE레포(머지+dist배포) · Figma · 기획PDF(초안 완성)  → 구글폼
[초대 필수]  eulji@likelion.org → GitHub(조직) · Figma · Notion       (본인 직접)
[초대 확인]  Google Drive · Slack — 실제 사용 시에만
[초대 불필요] AI Studio(개인 키) · EC2 서버(영수증만 추후)
[추후]       PPT(7/19) · 서버영수증(~7/26)
[확인]       노션 최신화 · FE PR 머지 · dist 라이브 동작 · 기획PDF 내용 검수
⚠️ 제출 후 수정 = 실격. 초대/폼제출/머지는 본인 클릭.
```

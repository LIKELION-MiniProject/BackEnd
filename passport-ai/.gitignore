# 시크릿 — 절대 커밋 금지
.env

# 파이썬 아티팩트
__pycache__/
*.pyc
.venv/
venv/

# 생성된 캐시 — 원칙적으로 커밋 제외
cache/*.json
# 단, 발표 재현용 데모 학생 캐시는 예외로 커밋 (Spring 이 오프라인으로 서빙)
!cache/202312345.json
# 라이브 브릿지의 데이터 지문(로컬 상태) — 커밋 금지. clone 시엔 없어도 안전 마이그레이션 동작.
cache/*.fp
